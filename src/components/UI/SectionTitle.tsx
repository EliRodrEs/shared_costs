
const SectionTitle = ({ sec_title }: { sec_title: string}) => {
  return (
    <>
      <div className="title">
        <h1>{sec_title}</h1>
      </div>
    </>
  );
};

export default SectionTitle
